<!DOCTYPE html>
<html lang="en_US">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <script src="/js/favoriteRecipe.js" defer></script>
    <script src="/js/showToast.js" defer></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <title>{{title}}</title>
</head>
<body>
    {{>navbar}}

    <div class="container content border p-3">
        <div class="row">
            <div class="col-md-6">
                <img src="{{recipe.image}}" class="img-fluid mb-4" alt="{{recipe.name}} Image">
            </div>
            <div class="col-md-6">
                <h2>{{recipe.name}}</h2>
                <button id="favoriteRecipe" type="button" class="btn btn-outline-warning mb-3 mt-2">
                    <i class="bi bi-star"></i> Recipe
                </button>
                <span class="d-none" id="recipeId">{{recipe.id}}</span>
                <p><strong>Category:</strong> {{recipe.category.name}}</p>
                <p><strong>Area:</strong> {{recipe.area.name}}</p>
                <p><strong>Preparation Time:</strong> {{recipe.preparationTime}}</p>
                <p><strong>Difficulty:</strong> {{recipe.difficulty.name}}</p>
                <p><strong>Author:</strong> {{recipe.author.username}}</p>
                <p><strong>Cost:</strong> {{recipe.cost}}</p>
                <p><strong>Description:</strong> {{recipe.description}}</p>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col">
                <h3>Preparation</h3>
                <p>{{recipe.preparationDescription}}</p>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col">
                <h3>Ingredients</h3>
                <div class="row">
                    {{#recipe.ingredients}}
                    <div class="col-md-4 mb-3">
                        <div class="list-group-item">
                            <div class="row align-items-center border">
                                <div class="col-md-4">
                                    <img src="{{ingredient.image}}" class="img-fluid" alt="{{ingredient.name}} Image">
                                </div>
                                <div class="col-md-8">
                                    <strong>{{ingredient.name}}</strong>
                                    <p>{{quantity}}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    {{/recipe.ingredients}}
                </div>
            </div>
        </div>
    </div>
    {{>toast}}
    {{>footer}}
</body>
</html>
