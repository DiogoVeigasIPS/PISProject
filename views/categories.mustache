<!DOCTYPE html>
<html lang="en_US">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>{{title}}</title>
</head>
<body>
    {{>navbar}}

    <div class="container content border p-3 mt-4">
        <div class="row row-cols-1 row-cols-md-4 g-4" id="category-container">
            {{#categories}}
            <div class="col">
                <div class="card category-card">
                    <img src="{{image}}" class="card-img-top" alt="{{name}} Image">
                    <div class="card-body">
                        <h5 class="card-title">{{name}}</h5>
                        <a href="/?category={{id}}" class="btn btn-primary">Explore Recipes</a>
                    </div>
                </div>
            </div>
            {{/categories}}
            {{^categories}}
                <p>No categories available at the moment.</p>
            {{/categories}}
        </div>
    </div>

    {{>footer}}
</body>
</html>
